<view class="cart-container">
    <!-- 头部：购买历史入口 (卡片风格) -->
    <view class="history-bar hover-class" bindtap="goToHistory" hover-class="history-bar-hover">
        <view class="history-left">
            <van-icon name="orders-o" size="20px" color="#FF5700" class="history-icon"/>
            <text class="history-text">我的购买历史</text>
        </view>
        <view class="history-right">
            <text class="history-desc">查看已买到的羊</text>
            <van-icon name="arrow" color="#ccc" />
        </view>
    </view>

    <!-- 购物车列表区域 -->
    <view class="cart-title">
      <text class="title-text">购物车</text>
      <text class="count-text">共 {{cartItems.length}} 件商品</text>
    </view>

    <!-- 空购物车提示 -->
    <block wx:if="{{cartItems.length === 0}}">
        <view class="empty-cart">
            <image class="empty-image" src="/images/icons/function/f1.png" mode="aspectFit"></image>
            <text class="empty-title">购物车是空的</text>
            <text class="empty-hint">领养一只羊，它就会出现在这里哦~</text>
            <button class="go-adopt-button" bindtap="goHome">去领养</button>
        </view>
    </block>

    <!-- 购物车列表 -->
    <block wx:else>
        <view class="cart-list">
            <block wx:for="{{cartItems}}" wx:key="cart_item_id">
                <view class="cart-item">
                    <!-- 复选框 -->
                    <view class="checkbox-wrap" bindtap="toggleSelect" data-index="{{index}}">
                        <view class="checkbox {{item.selected ? 'checkbox-checked' : ''}}">
                            <text wx:if="{{item.selected}}" class="checkbox-tick">✓</text>
                        </view>
                    </view>

                    <!-- 左侧图片 -->
                    <image class="cart-image" src="{{item.sheep.image || '/images/icons/function/f1.png'}}" mode="aspectFill"></image>
                    
                    <!-- 右侧详情 -->
                    <view class="cart-details">
                        <view class="cart-header">
                            <text class="sheep-name">滩羊 #{{item.id}}</text>
                            <van-icon name="delete-o" size="18px" color="#999" bindtap="deleteItem" data-id="{{item.id}}" data-cart-item-id="{{item.cart_item_id}}" class="delete-icon" />
                        </view>
                        
                        <view class="sheep-tags">
                            <text class="tag">{{item.gender}}</text>
                            <text class="tag">{{item.weight}} kg</text>
                        </view>

                        <view class="cart-footer">
                            <view class="price-box">
                                <text class="price-symbol">¥</text>
                                <text class="price-text">{{item.price}}</text>
                            </view>
                            <view class="quantity-box">
                                <text class="qty-label">x</text>
                                <text class="qty-text">{{item.quantity || 1}}</text>
                            </view>
                        </view>
                    </view>
                </view>
            </block>
        </view>
    </block>

    <view class="checkout-bar-wrapper" wx:if="{{cartItems.length > 0}}">
        <view class="checkout-bar">
            <!-- 左侧：全选 + 合计 -->
            <view class="bar-left">
                <view class="select-all-wrap" bindtap="toggleSelectAll">
                    <view class="checkbox {{allSelected ? 'checkbox-checked' : ''}}">
                        <text wx:if="{{allSelected}}" class="checkbox-tick">✓</text>
                    </view>
                    <text class="select-all-text">全选</text>
                </view>
                <view class="total-info">
                    <text class="total-label">合计:</text>
                    <text class="total-symbol">￥</text>
                    <text class="total-price">{{totalPrice}}</text>
                </view>
            </view>
            <!-- 右侧：结算按钒 -->
            <button class="checkout-button hover-class" bindtap="checkout" hover-class="checkout-hover">马上结算({{selectedCount}})</button>
        </view>
    </view>

    <!-- 支付方式弹出面板 -->
    <van-action-sheet
      show="{{ showPaymentSheet }}"
      title="选择支付方式"
      bind:close="onClosePaymentSheet"
    >
      <view class="payment-options">
        <van-cell-group>
          <!-- 余额支付 -->
          <van-cell 
            title="余额支付" 
            label="可用余额: ¥{{userBalance}}" 
            icon="gold-coin-o" 
            clickable 
            is-link
            data-method="balance"
            bind:click="onSelectPayment"
          />

          <!-- 微信支付 -->
          <van-cell 
            title="微信支付" 
            label="暂未开通真实支付" 
            icon="wechat" 
            clickable
            is-link 
            data-method="wechat"
            bind:click="onSelectPayment"
          />
        </van-cell-group>
      </view>
    </van-action-sheet>

    <van-dialog id="van-dialog" />
</view>
